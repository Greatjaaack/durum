# У бота своего Caddy НЕТ — на сервере уже работает общий Caddy от аналитики
# (он держит порты 80/443). Чтобы он отдавал ещё и дашборд бота, добавьте этот
# блок в Caddyfile АНАЛИТИКИ и перезагрузите её Caddy (см. DEPLOY.md).
#
# bot.iskendy.ru {
# 	encode gzip
# 	reverse_proxy durum-dashboard-1:8000
# }
