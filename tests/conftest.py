"""Общая конфигурация pytest для проекта.

В проекте нет «бизнес» окружения у тестов — мы тестируем чистую логику
(YAML-конфиги, сборку wizard-структур, миграции схемы на временной БД).
Этот файл нужен только для того, чтобы выставить минимальные переменные
окружения, которые при импорте пытаются прочитать модули приложения,
но мы не хотим тащить .env в тестах.
"""
from __future__ import annotations

import os


# Минимальные «болванки» для load_settings(): тесты не вызывают реальный
# Telegram, но импорт модулей может тронуть config через побочные пути.
os.environ.setdefault("BOT_TOKEN", "test-token")
os.environ.setdefault("OWNER_ID", "1")
os.environ.setdefault("BOT_TIMEZONE", "Europe/Moscow")
